# media/audio/ — `<creator>/<title>.<ext>`

What an item's `audio:` field points at. Written by `inductor run`, as symlinks
into wherever the originals live unless `media.mode` says otherwise.

Not in git. See [`../README.md`](../README.md) for why, and for what to use
instead.
